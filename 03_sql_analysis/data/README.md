# Dataset

Используется тот же `global_ecommerce_sales.csv`, что в Project 01.

Период: **2023–2025**. В PostgreSQL импортируется в таблицу `ecommerce_sales` по схеме из `sql/00_schema.sql`.

Такой подход не создаёт фиктивную реляционную модель поверх плоского исходника.
